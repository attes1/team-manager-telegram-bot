services:
  team-manager-bot:
    build: .
    container_name: team-manager-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Europe/Helsinki
